export enum TourStatus {
	APPROVED = "approved",
	PENDING = "pending",
	BLOCKED = "blocked",
	DEPARTED = "departed",
	REQUESTED = "requested"
}

export enum TourTypes {
	DAY_TRIP = "day-trip",
	MULTI_DAY = "multi-day"
}
